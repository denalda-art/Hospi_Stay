import pandas as pd

def rm_main(data):
    # Print to check data is received
    print("Data received from RapidMiner:")
    print(data.head())

    # Example: Just return the same data
    result = data.copy()
    
    return result

import joblib

# Assuming your trained model is stored in a variable (example: `trained_model`)
joblib.dump(trained_model, "/Users/denaldakuzumi/RapidMinerScripts/model.pkl")
print("Model saved successfully!")


